export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface ITabPanel {
  id: number;
  img: string;
  imfo: string;
}
export interface INav {
  id: number;
  title: string;
  path: string;
}
